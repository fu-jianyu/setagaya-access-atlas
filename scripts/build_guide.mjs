import fs from 'node:fs';import path from 'node:path';import {fileURLToPath} from 'node:url';import {PRESETS} from '../dist/model.mjs';
const root=path.resolve(path.dirname(fileURLToPath(import.meta.url)),'..'),out=path.join(root,'dist');
const esc=s=>String(s).replaceAll('&','&amp;').replaceAll('<','&lt;').replaceAll('>','&gt;');
const keys=['id','name','domain','networkMetric','supply','kernel','radius','beta','shape','exponent','gamma','a','tau','rank','lower','b','threshold','p','speed'];
const rows=PRESETS.map(p=>keys.map(k=>p[k]??p.params[k]??''));const csv=[keys,...rows].map(row=>row.map(x=>'"'+String(x).replaceAll('"','""')+'"').join(',')).join('\r\n');fs.writeFileSync(path.join(out,'data','indicator-guide.csv'),'\ufeff'+csv);fs.writeFileSync(path.join(out,'data','presets.json'),JSON.stringify(PRESETS,null,2));
const table=PRESETS.map(p=>`<tr><td><b>${p.id}</b><br>${esc(p.name)}</td><td>${p.params.domain==='network'?'All buffered junctions':'Selected POI category'}</td><td><code>${esc(p.formula)}</code></td><td>${esc(p.description)}</td></tr>`).join('');
const parameters=[['R','Walking radius','Increase to include more distant places. For a fixed nonnegative, noncompetitive catchment kernel, reach cannot fall as R increases. 65 on the slider represents infinity.'],['β','Distance sensitivity','For K=exp(−βcˢ), larger β reduces the contribution of a fixed positive cost. With competition in both steps, the final score need not decrease. β has units min⁻ˢ.'],['s','Decay shape','1 gives exponential decay; 2 gives Gaussian decay. Intermediate settings create a stretched-exponential family.'],['α','Power exponent','K=c⁻ᵅ. Larger α changes the preference for nearer relative to farther places. Costs use minutes; absolute magnitudes depend on this unit. Zero cost is undefined.'],['γ','Competition strength','The opportunity mass is qⱼDⱼ⁻ᵞ. At 0, ignore demand. At 1, use supply-to-demand weighting. Raising γ increases the relative preference for lower-D facilities; absolute change depends on demand units.'],['a','Quantity emphasis','The spectral readout is multiplied by Wᵃ. With the full distribution and τ=0, a=1 yields a total and a=0 a weighted mean.'],['τ','Quota emphasis','For cost readout, 0 uses the average and 1 the cost to reach quota r. Intermediate settings blend these two quantities.'],['r','Opportunity requirement','Absolute mass, not a percentile. With q=1 and no competition, integer r returns the rth closest facility. With type weights or competition, r is an effective opportunity quantity.'],['l','Hard-to-reach share','The readout uses quantiles l through 1. For example, l=.8 evaluates the hardest-to-reach 20% of opportunity mass.'],['b / t','Response and threshold','b=1 keeps the readout; b=−1 takes its reciprocal; b=0 returns 1 when the readout is at least t, otherwise 0. For a cost, this is a threshold exceedance, not a benefit.'],['p','Street aggregation','1 is the arithmetic mean, 0 geometric mean, negative values emphasise smaller scores, positive values larger scores. Slider endpoints −5 and 5 select minimum and maximum. A large cost is worse; a large benefit is better.'],['v','Walking speed','Convert metres to minutes using c=distance/(1000v/60). The default 4.8 km/h equals 80 m/min. This is constant-speed walking, not observed journey time.']];
const paramTable=parameters.map(r=>`<tr><td><b>${r[0]}</b></td><td>${r[1]}</td><td>${r[2]}</td></tr>`).join('');
const html=`<!doctype html><html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>Parameter guide / Access Atlas</title><link rel="icon" href="data:image/svg+xml,%3Csvg xmlns='http://www.w3.org/2000/svg'/%3E"><style>body{font:17px/1.7 'Segoe UI',Arial,sans-serif;color:#193a48;background:#f5f9fa;margin:0}main{max-width:1100px;margin:35px auto;background:white;padding:45px;border:1px solid #e0e9eb;border-radius:16px}h1{font-size:39px;line-height:1.2;margin:15px 0}h2{font-size:26px;margin-top:40px}p{max-width:900px}a{color:#107b80}code{font-size:15px;background:#eef6f7;padding:5px;border-radius:4px}table{width:100%;border-collapse:collapse;font-size:15px}td,th{padding:15px 13px;text-align:left;border-bottom:1px solid #e0e9eb;vertical-align:top}th{background:#eef6f7}td:first-child{min-width:120px}.formula{padding:24px;background:#eef7f7;border-radius:10px;font:19px/1.9 Georgia,serif}.tag{color:#6c838c;font-size:13px;letter-spacing:2px}.scroll{overflow:auto}li{margin:10px 0}@media(max-width:700px){main{padding:22px;margin:12px}h1{font-size:30px}}@media print{body{background:white}main{margin:0;border:0;padding:0}a{color:inherit}tr{break-inside:avoid}}</style></head><body><main><a href="./">← Back to the map</a><p class="tag">SETAGAYA / ACCESS ATLAS</p><h1>From parameters to a purpose.</h1><p>This guide explains all 17 built-in configurations and the meaningful combinations between them. Classic names describe formulas under the stated dataset and aggregation conventions; they do not imply that every implementation of a similarly named indicator gives the same value.</p><p><a href="data/indicator-guide.csv" download>Download the complete numerical configuration table (CSV)</a> · <a href="data/presets.json" download>Download presets (JSON)</a></p><h2>The common computation</h2><div class="formula">mᵢⱼ = qⱼ Dⱼ⁻ᵞ, &nbsp; Dⱼ = Σₒ nₒ G(cₒⱼ), &nbsp; Wᵢ = Σⱼ mᵢⱼ<br>Zᵢ = Wᵢᵃ { (1−τ)/(1−l) ∫ₗ¹ K[Tᵢ(uWᵢ)] du + τ K[Tᵢ(r)] }<br>Aₑ = Mₚ { Ψᵦ,ₜ(Zᵢ) : i is a sample of street e }</div><p>Tᵢ(r) is the walking cost needed to accumulate opportunity mass r. Ψ applies the direct, reciprocal or threshold response. Mₚ combines street samples. For a full distribution with τ=0, the integral reduces exactly to ΣⱼmᵢⱼK(cᵢⱼ)/Wᵢ. The browser uses this identity for efficient calculation.</p><p>Network centralities use a separately precomputed network target relation. Edge betweenness is the through-route configuration (λ=1), counting shortest paths through each physical edge. The site does not expose a continuous local/through mixture: those relations must first have a common target and scale. Its parameter controls only enable operations the built-in relation tables actually support.</p><h2>The 17 configurations</h2><p>Unless specified otherwise: γ=0, a=1, τ=0, l=0, b=1, p=1, speed=4.8 km/h; all facilities count equally. The CSV lists every numerical value. Inactive settings have no computational effect and their controls are disabled.</p><div class="scroll"><table><thead><tr><th>Indicator</th><th>Targets</th><th>Defining configuration</th><th>Question answered</th></tr></thead><tbody>${table}</tbody></table></div><h2>What each parameter does</h2><div class="scroll"><table><thead><tr><th>Parameter</th><th>Physical meaning</th><th>How to use it</th></tr></thead><tbody>${paramTable}</tbody></table></div><h2>Combinations worth exploring</h2><ol><li><b>Everyday convenience:</b> choose Daily shopping and Exponential gravity. Increase β to place more emphasis on short walks; watch the raw values as well as the relative colours.</li><li><b>A 15-minute neighbourhood:</b> choose Cumulative opportunities, set R=15, and compare the five POI categories. POI coverage varies between categories and OSM completeness also varies.</li><li><b>More choice, not just one destination:</b> choose Nth-nearest destination, then move r from 1 to 3 to 5. Required walking cost cannot decrease.</li><li><b>Competition-aware service access:</b> choose 2SFCA and adjust γ from 0 toward 1. This uses equal junction demand, not census population. The result illustrates competition mechanisms.</li><li><b>Protect the less-accessible end of a street:</b> choose Nearest destination, open More ways to measure and move p toward Max. Higher cost positions increasingly dominate.</li><li><b>Hard-to-reach opportunity burden:</b> choose Read walking cost, τ=0, a=0 and l=.8. The readout becomes the average cost of reaching the hardest 20% of opportunity mass.</li></ol><h2>Colour scale</h2><p>Blue represents lower numerical values and red higher numerical values for all indicators. The legend increases from left to right. For walking-time indicators, higher (redder) values mean longer journeys. The 5th–95th percentile display range does not change the underlying values.</p><h2>Data and interpretation limits</h2><p>Real OSM geometry and POIs are used on a simplified public-street skeleton with a surrounding buffer. Connections are to the nearest retained junction with a straight access link of at most 350 m. These are not entrance-level pedestrian routes. Distances are cached as float32 metres. The displayed street is assigned to the ward by its midpoint; a boundary-crossing street can extend slightly outside the ward.</p><p>POI metrics aggregate two endpoints, with a midpoint added on streets longer than 200 m. Network mean depth, closeness and harmonic centrality instead map precomputed junction centralities to their two endpoints. Native junction centrality and the displayed street aggregation are distinct. These formulas are not presented as space-syntax integration, NAIN or NACH.</p><p>Supply weights are scenarios (1–4 units per facility). Demand is one unit at every retained inside-ward junction. Neither measured population nor facility capacity is present. Equal-name, same-type POIs within 90 m are deduplicated heuristically. Closed degree-two loops and isolated components are not represented in the retained skeleton. The results are an exploratory calculation, not a validated ranking of actual service provision.</p><h2>Verification</h2><p>Built-in surfaces use independent, vectorized reference definitions. The interactive evaluator is checked against these on sampled real streets across all five categories, plus random mathematical fixtures and monotonicity checks. The project includes the source data, rebuild scripts and audit results. Matching formulas validates computation; it does not validate the modelling assumptions.</p><h2>Primary sources</h2><ul><li><a href="https://www.openstreetmap.org/copyright">OpenStreetMap contributors, ODbL 1.0</a></li><li><a href="https://maps.gsi.go.jp/development/">Geospatial Information Authority of Japan: map tile catalogue</a></li><li><a href="https://www.niu.edu/landform/papers/Luo_Wang2003.pdf">Luo & Wang (2003), two-step floating catchment access</a></li><li><a href="https://www.niu.edu/landform/papers/JHAP741_e2sfca.pdf">Luo & Qi (2009), enhanced two-step floating catchment access</a>. This app uses bands 1 / .42 / .09, explicitly. It does not silently substitute the later .03 convention.</li><li><a href="https://networkx.org/documentation/stable/reference/algorithms/centrality.html">NetworkX: centrality definitions</a></li><li><a href="https://link.springer.com/chapter/10.1007/978-3-030-59140-3_2">van Nes & Yamu (2021): space syntax definitions and normalisation distinctions</a></li></ul></main></body></html>`;fs.writeFileSync(path.join(out,'guide.html'),html);
console.log('English parameter guide, CSV and full JSON configurations written.');
